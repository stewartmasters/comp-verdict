node_modules/

# Large downloaded source files (re-fetched by pipeline, cached in CI)
data/bls/
data/ons/
data/ine/

# Stack Overflow survey CSV (too large for git, user downloads manually)
data/so_survey/*.csv

# Keep the DB — it's the historical record and relatively small
# If it grows large, move to git-lfs
# data/pipeline.db
