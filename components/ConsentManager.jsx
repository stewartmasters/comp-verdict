'use client'

/**
 * ConsentManager — cookie consent banner.
 * Stub: renders nothing until a consent management provider is integrated.
 * Replace with a real CMP (e.g. Cookiebot, Osano) when needed.
 */
export default function ConsentManager() {
  return null
}
